
def lcm(a, b):
  """compute Least Common Multiple between two integers."""
  if a > b:
    big = a
  else:
    big = b
  while(True):
    if((big % a== 0) & (greater % b == 0)):
     lcm = big
     break
    breater += 1
  return lcm
  
